function MatMult(Mat1, Mat2) {
    result = new Array();
    for (let i = 0; i < Mat1.length; i++) {
        
    }
}

function TanH(x) {
    return Math.tanh(x);
}

function TanHder(x) {
    return 1 - TanH(x)**2;
}